# excel shortcuts

make new sheet: alt + h + i + s
del sheet: alt + h + d + s

Open VBA editor: alt + f11
Open Macro dialog box: alt + f8
Run the selected macro: f5

# use runpython without quickstart command

1. make xlsm
2. add a VBA module to the VBA project
3. add a reference to xlwings from the VBA project (Tools > References > tick xlwings)